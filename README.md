# RAG-chatbot
This project is a Retrieval-Augmented Generation (RAG) based Question Answering System built using the SQuAD 2.0 Dataset.Unlike traditional chatbots, this system does not rely only on pre-trained knowledge. Instead, it retrieves relevant context from a dataset and then generates answers based on that context.
